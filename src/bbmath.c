#include "math.h"

/**
 * Summary: used to get a complete factor list of a number
 * int num: the number to be factored
 * int* factors: an array to contain all the factors, must be the appropriate length, otherwise behavior undefined
 * Use factorCount to get the number of factors to be stored in int* factors
 **/
void factorial(int num, int* factors)
{

}

int imin(int first, int second)
{
	return first < second ? first : second;
}

int imax(int first, int second)
{
	return first > second ? first : second;
}

/*
void addTo(byte* source, unsigned int slength, byte* destination, unsigned int dlength)
{
	const int min_length = slength < dlength ? slength : dlength;


}
*/